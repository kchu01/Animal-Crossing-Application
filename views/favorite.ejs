<h1>Favorites</h1>

// figure out how to pull data into here
<!-- <% // console.log(villagers) %>
    <%= // villagers.name %> -->

<% villagers.forEach(villager=> { %>
    <h3>
        <%= villager.name %>
    </h3>
    <% }) %>