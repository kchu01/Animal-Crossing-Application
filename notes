install packages/set up basic html and css
set up inital route ('/') main index
set up user routes first (login/logout)
set up api and villagers routes




bugs to fix:
* solved - villager details shows up on console but not rendering
* solved - household_items can grab data from console but cannot grab name 
* solved - user is not defined - navbar separation
* solved - favorites page doesnt take in data sent by villagerController

* correct all householditem to item
* items show details page isnt rendering
* item favorites not working

to do:
* delete route
* put route - update
* one stretch goal
* update readme



models:

sequelize model:generate --name=user --attributes username:string,password:string

sequelize model:generate --name=users_villager --attributes userId:integer,villagerId:integer

sequelize model:generate --name=villager --attributes name:string

sequelize model:generate --name=item --attributes name:string

sequelize model:generate --name=users_item --attributes userId:integer,itemId:integer